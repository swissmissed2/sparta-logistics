server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: #
          default-label: dev # 기본 브랜치 지정
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
